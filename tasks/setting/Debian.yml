---
# code: language=ansible
# - name: Create/update setting file
#   ansible.builtin.template:
#     src: meilisearch.conf.j2
#     dest: /erc/meilisearch/meilisearch.conf
#     mode: "0644"
# - name: Add/update option setting
#   ansible.builtin.lineinfile:
#     path: /etc/meilisearch/meilisearch.conf
#     regexp: ^(#\\s+)?option_name=
#     line: option_name=option_value
# - name: Add/update custom setting
#   ansible.builtin.blockinfile:
#     path: /etc/meilisearch/meilisearch.conf
#     block: "{{ meilisearch_extra_cfg | default('') }}"
